# Data Assets

Expected files:

- `output_graph.ttl`: RDF knowledge graph in Turtle format.
- `questions_set_easy.json`: Curated question set used for the notebook and batch runner.

The full graph file was not present in this workspace when the repo was packaged. Add it at `data/output_graph.ttl` before running `scripts/run_query.py` or `scripts/run_batch.py`.
